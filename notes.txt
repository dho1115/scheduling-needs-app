Just added another RESTful operation (PUT request):

Promise.all([
   PostRequest('http://localhost:3003/shiftsPendingEmployeeConfirm', { id: shiftID, storeNumber, _candidateID: id, candidate: name }),
   PutRequest('http://localhost:3003/employees', updatedEmployees)
])

PutRequest did NOT properly update employee: Please consider using PutRequest('http://localhost:3003/employees?id=c-mbwm1j9c' logic to remove the shift goes here. Perhaps a shiftsAppliedFor: [shiftsAppliedFor.filter])